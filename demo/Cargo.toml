[package]
name = "mcaux_ct_demo"
version = "0.1.0"
authors = ["Steve Work <steve@work.renlabs.com>"]
edition = "2024"
include = ["LICENSE-APACHE", "LICENSE-MIT", "**/*.rs", "Cargo.toml"]
rust-version = "1.88"

[package.metadata.docs.rs]
all-features = true
targets = ["x86_64-unknown-linux-gnu", "wasm32-unknown-unknown"]

[features]
not-no-std = []

[dependencies]
egui = "0.33.0"
eframe = { version = "0.33.0", default-features = false, features = [
    "default_fonts", # Embed the default egui fonts.
    "glow",          # Use the glow rendering backend. Alternative: "wgpu".
    "persistence",   # Enable restoring app state when restarting the app.
    "wayland",       # To support Linux (and CI)
    "x11",           # To support older Linux distributions (restores one of the default features)
] }

# You only need serde if you want app persistence:
serde = { version = "1.0.219", features = ["derive"] }
log = "0.4.28"

# native:
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
env_logger = "0.11.8"
momentary = { version = "0.1.0", path = "../momentary", features = ["not-no-std"] }
indicators = { version = "0.1.0", path = "../mcaux-indicators" }

# web:
[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen-futures = "0.4.50"
web-sys = "0.3.70"              # to access the DOM (to hide the loading text)
web-time = "1.1.0"
momentary = { version = "0.1.0", features = ["wasm"], path = "../momentary" }
indicators = { version = "0.1.0", default-features = false, path = "../mcaux-indicators" }


